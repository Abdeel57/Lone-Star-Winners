/**
 * `@lsw/audit` - ANDAMIAJE.
 *
 * Contiene tipos y el catalogo de acciones auditables. La hash chain de
 * DEC-008, el verificador y el sellado externo diario llegan cuando exista el
 * esquema del ledger (`packages/database`, propiedad de `backend`).
 */

export * from "./types.js";
export * from "./actions.js";
